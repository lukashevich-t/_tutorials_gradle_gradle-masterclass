logger.quiet """==========================================
Initialization phase: processing settings.gradle.
=========================================="""
logger.quiet "timestamp = ${gradle.timestamp()}"
logger.quiet "gradle version $gradle.gradleVersion"
logger.quiet "gradle UserHomeDir ${gradle.gradleUserHomeDir}"
logger.quiet "gradle HomeDir $gradle.gradleHomeDir"
logger.quiet "gradle parent ${gradle.parent}"
logger.quiet "gradle Start Parameter ${gradle.getStartParameter()}"