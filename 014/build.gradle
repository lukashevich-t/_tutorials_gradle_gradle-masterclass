logger.info "Info: Hello"

apply {
  println "apply: delegate.class is ${delegate.class}"
  println "apply: this.class is ${this.class.canonicalName}"
  println "apply: it.class is ${it.class}"
}